debug: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jpa-study
    username: root
    password: password
  jpa:
    properties:
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
